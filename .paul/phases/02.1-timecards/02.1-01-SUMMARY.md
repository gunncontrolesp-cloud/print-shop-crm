---
phase: 02.1-timecards
plan: 01
subsystem: ui
tags: [timeclock, supabase, rls, server-actions, next-js]

requires:
  - phase: 02-production
    provides: jobs table (job_id FK for future job-linked time entries)
  - phase: 06-saas
    provides: tenant isolation pattern (get_my_tenant_id, is_authenticated RLS helpers)

provides:
  - time_entries table with tenant-isolated RLS
  - clockIn / clockOut server actions
  - /dashboard/timeclock employee page

affects: [02.1-02-admin-dashboard, 02.1-03-notifications-reports]

tech-stack:
  added: []
  patterns: [server-action-form, tenant-rls]

key-files:
  created:
    - supabase/migrations/20260402000001_timecards.sql
    - src/lib/actions/timeclock.ts
    - src/app/dashboard/timeclock/page.tsx
  modified:
    - src/lib/types.ts
    - src/components/nav-sidebar.tsx

key-decisions:
  - "clockIn accepts _formData as first param to satisfy Next.js form action type signature"
  - "Job selection deferred to 02.1-02 — plain clock in only for this plan"

patterns-established:
  - "time_entries RLS follows same tenant isolation pattern as all other tables"

duration: ~1 session
started: 2026-04-02T00:00:00Z
completed: 2026-04-02T00:00:00Z
---

# Phase 2.1 Plan 01: Employee Time Clock Summary

**time_entries table with RLS + clockIn/clockOut server actions + mobile-friendly /dashboard/timeclock page shipped and verified working in production.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~1 session |
| Tasks | 3 completed + 1 checkpoint |
| Files modified | 5 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Employee can clock in | Pass | Clock In button creates open time_entry row |
| AC-2: Employee can clock out | Pass | Clock Out sets clocked_out_at, page reloads to clocked-out state |
| AC-3: Duplicate clock-in blocked | Pass | clockIn checks for open entry, throws 'Already clocked in' |
| AC-4: RLS isolates tenant data | Pass | All policies use get_my_tenant_id() |

## Accomplishments

- `time_entries` table created with full tenant-isolated RLS (5 policies)
- `clockIn` / `clockOut` server actions with duplicate-prevention guard
- `/dashboard/timeclock` mobile-friendly page: green/gray status card, large tap-target buttons, recent entries table with duration calculation
- "Time Clock" added to dashboard nav sidebar

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `supabase/migrations/20260402000001_timecards.sql` | Created | time_entries table + RLS |
| `src/lib/actions/timeclock.ts` | Created | clockIn / clockOut server actions |
| `src/app/dashboard/timeclock/page.tsx` | Created | Employee clock in/out UI |
| `src/lib/types.ts` | Modified | Added TimeEntry type |
| `src/components/nav-sidebar.tsx` | Modified | Added Time Clock nav link |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| `clockIn(_formData?, jobId?, taskStage?)` — FormData as first param | Next.js requires form action functions to accept FormData as first argument | Job/stage still available for programmatic calls in 02.1-02 |
| No job selection UI on clock-in | Deferred to 02.1-02 per scope boundaries | job_id column exists in schema, ready for wiring |

## Deviations from Plan

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 1 | TypeScript fix, no scope change |
| Scope additions | 1 | Nav link added (minor, necessary) |

**Auto-fix:** `clockIn` signature needed `_formData?: FormData` as first param — Next.js enforces this for server actions used in `<form action={...}>`. Fix applied during Task 2 qualify loop.

**Scope addition:** `nav-sidebar.tsx` not listed in plan's `files_modified` but required for the page to be reachable. Essential, not scope creep.

## Next Phase Readiness

**Ready:**
- `time_entries` table live in Supabase with correct RLS
- `clockIn` / `clockOut` actions available for 02.1-02 admin UI to query
- TimeEntry type exported from types.ts

**Concerns:**
- None

**Blockers:**
- None

---
*Phase: 02.1-timecards, Plan: 01*
*Completed: 2026-04-02*
